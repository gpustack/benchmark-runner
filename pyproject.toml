[project]
name = "benchmark-runner"
version = "v0.0.0"
description = "Benchmark Runner"
authors = [{name = "GPUStack Authors", email = "contact@gpustack.ai"}]
readme = "README.md"
requires-python = ">=3.10,<3.13"
keywords = [
    "ai",
    "benchmarking",
    "deployment",
    "evaluation",
    "guidance",
    "inference",
    "language-models",
    "large-language-model",
    "llm",
    "model-benchmark",
    "model-evaluation",
    "nlp",
    "performance",
    "gpustack",
    "guidellm"
]
dependencies = [
    # Upper-bounded: the runner forks guidellm 0.5.x internals
    # (guidellm.backends.response_handlers, GenerationResponseHandlerFactory),
    # which 0.6.0 restructured/removed. Bump intentionally after porting.
    "guidellm>=0.5.1,<0.6",
    # Required by some models' tokenizers (e.g. Kimi-K2) for synthetic_text
    # dataset deserialization. Not pulled in transitively by guidellm. (#5689)
    "tiktoken",
]

[dependency-groups]
dev = [
    "pre-commit>=3.7.1",
    "flake8>=7.0.0",
    "flake8-bugbear>=24.4.26",
    "black>=24.4.2",
    "pytest>=8.2.0"
]

[project.scripts]
benchmark-runner = "benchmark_runner.main:cli"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
only-include = ["benchmark_runner"]

[tool.black]
line-length = 88
target-version = ['py310']
skip-string-normalization = true
force-exclude = '''
/(
    .*/migrations/.*
)/
'''
